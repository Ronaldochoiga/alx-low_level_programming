#include "main.h"

/**
 *print_sign - print signs
 *
 *@n: parameter
 *
 *Return: always 0
 **/
int print_sign(int n)
{
	if (n > 0)
		return (1);
	_putchar('+');
	else if (n == 0)
		return (0);
	putchar(0);
	else
		return (-1);
	putchar('-');
}
