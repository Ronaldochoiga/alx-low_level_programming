#include "main.h"

/**
 *print_alphabet_x10 - ptints 10 times
 *
 *Return: always 0
 *
 **/
void print_alphabet_x10(void)
{
	char i;

	for (i = 'a'; i <= 'z' ; i++)
	{
		if (i == 'z' && 'z' <= 10)
		{
			_putchar(i);
		}
	}
	putchar('\n');
}
