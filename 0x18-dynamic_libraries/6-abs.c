#include "main.h"

/**
 * _abs - computes the real value of integers
 * @n: the int to be checked
 * Return: the absolute value of integer
 */
int _abs(int n)
{
  if (n >= 0)
    {
      return (n);
    }
  return (-n);
}
