#include "main.h"

/**
 *cap_string - main function
 *
 *@s: functional parameter
 *
 *Return: always 0
 *
 */
char *cap_string(char *s)
{
	int i;
	int j[i + 1];

	char a[] = "32, 9, 10, 44, 59, 46, 33, 63, 40, 41, 123, 125";

	for (i = 0 ; s[i] != '\0' ; i++)
	{
		if (i == a && 97 >= j <= 122)
			s[i - 32] = j;
	}
	return (s)
}
